
events {}

http {
    server {
        listen 80;

        location / {
            root /opt/calendar/public;
            charset utf-8;
        }

        location /uploads {
           root /var/calendar/uploads;
        }

        location /v1 {
            proxy_pass http://api:5000/v1;
        }    
    }
}